[project]
name = "chef"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["rich", "prompt_toolkit"]

[project.scripts]
chef = "chef.main:main"
